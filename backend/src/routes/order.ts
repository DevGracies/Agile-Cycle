import { Router } from "express";
// import { adminOnly, authenticate } from "../middlewares/auth";
// import { getAllOrders, updateOrderStatus, getOrderDashboardStats } from "../controllers/order";

const router = Router();

// router.use(authenticate);
// router.get("/customers", getAllCustomerOrders);
// router.get("/:id", getOrder);
// router.post("/", createOrder);
// router.delete("/:id", cancelOrder);

// router.use(adminOnly) // ADMIN ACCESS ONLY
// router.get("/all", getAllOrders);
// router.patch("/:id/status", updateOrderStatus);
// router.get("/stats", getOrderDashboardStats);

export default router;